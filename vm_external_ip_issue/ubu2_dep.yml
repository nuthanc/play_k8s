apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubu2-deployment
  namespace: ns1
spec:
  selector:
    matchLabels:
      app: ubu2-deployment
  template:
    metadata:
      labels:
        app: ubu2-deployment
    spec:
      containers:
      - name: ubuntu-pod
        image: ubuntu
  replicas: 4